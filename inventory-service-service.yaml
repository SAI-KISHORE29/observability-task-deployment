apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  labels:
    app: inventory-service
spec:
  type: LoadBalancer
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000